import React from "react";

const SubmissionDetailsPage = ({ submission }) => {
  return (
    // <div>
    //   <h1>{submission.title}</h1>
    //   <p>{submission.summary}</p>
    //   <p>{submission.description}</p>
    //   <img src={submission.coverImage} alt={submission.title} />
    //   <p>{submission.hackathonName}</p>
    //   <p>{submission.hackathonStartDate}</p>
    //   <p>{submission.hackathonEndDate}</p>
    //   <a href={submission.githubRepoLink}>GitHub Repo Link</a>
    //   {submission.otherLinks.map((link, index) => (
    //     <a key={index} href={link}>
    //       Other Link {index + 1}
    //     </a>
    //   ))}
    // </div>
    <div>   </div>
  );
};

export default SubmissionDetailsPage;
